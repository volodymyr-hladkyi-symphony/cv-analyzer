management.endpoints.web.exposure.include=health,info,metrics
management.endpoint.health.show-details=always
spring.application.name=cv-analyzer
spring.ai.ollama.enabled=false
# Exclude Ollama autoconfiguration when using OpenAI (default profile)
spring.autoconfigure.exclude=org.springframework.ai.model.ollama.autoconfigure.OllamaChatAutoConfiguration
spring.ai.openai.api-key=${OPENAI_API_KEY}
spring.ai.openai.chat.options.model=gpt-4o
spring.ai.openai.chat.options.temperature=0.1

# Logging Configuration for LLM Responses
logging.level.org.springframework.ai=DEBUG
logging.level.org.springframework.ai.chat=DEBUG
logging.level.org.springframework.ai.chat.client=DEBUG
logging.level.org.springframework.ai.chat.client.advisor=DEBUG
logging.level.org.springframework.ai.observability=DEBUG

# Spring AI Observability Configuration
# Note: Spring AI metrics are automatically enabled when using ChatClient/ChatModel
# Built-in metrics should be available at:
# - /actuator/metrics/gen_ai.client.operation (if available)
# - /actuator/metrics/gen_ai.client.token.usage (if available)
# Verify available metrics by calling: GET /actuator/metrics

# Prompt locations (can be overridden to external file paths like file:/etc/cv-analyzer/prompts/...)
# prompts.summary.system=classpath:prompts/summary/system.txt
# prompts.summary.user=classpath:prompts/summary/user.txt
# prompts.rating.system=classpath:prompts/rating/system.txt
# prompts.rating.user=classpath:prompts/rating/user.txt

# Admin basic auth (change in production or via env vars ADMIN_USERNAME/ADMIN_PASSWORD)
admin.username=${ADMIN_USERNAME:admin}
admin.password=${ADMIN_PASSWORD:admin}

# OpenAI Pricing Configuration (per 1M tokens)
# Based on https://openai.com/api/pricing/ - GPT-4o pricing
openai.pricing.input-tokens-per-million=${OPENAI_PRICING_INPUT:0.1}
openai.pricing.output-tokens-per-million=${OPENAI_PRICING_OUTPUT:0.4}
openai.pricing.currency=${OPENAI_PRICING_CURRENCY:USD}

# Candidate Rating Configuration
candidate.rating.min=${CANDIDATE_RATING_MIN:1}
candidate.rating.max=${CANDIDATE_RATING_MAX:100}